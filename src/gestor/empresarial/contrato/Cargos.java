package gestor.empresarial.contrato;

public enum Cargos { //El enum representa una enumeracion, realiza la definicion de los propios tipos de datos del usuario
    confianza, //Atributo, cuado no tiene nada al inicio significa que es publico.
    sindicalizado, //Atributo
    temporal, //Atributo
}
